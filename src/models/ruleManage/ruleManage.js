import { addNote } from '@/services'

export default  {
  namespace: 'ruleManager',

  state: {
    type:'height'
  },

  effects: {
    *query({ payload }, { call, put }) {},

  },
  reducers: {
  }
};

